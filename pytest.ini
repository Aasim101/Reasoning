[pytest]
testpaths = tests
addopts = -ra --strict-markers
markers =
    network: requires internet access; skipped unless HARNESS_ALLOW_NETWORK=1
    gpu: requires a CUDA device; skipped on CPU-only machines
filterwarnings =
    default
    ignore::DeprecationWarning:pkg_resources.*
